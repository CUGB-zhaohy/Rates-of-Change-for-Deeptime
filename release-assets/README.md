# Local release assets

`RoC_Workflow_Windows_v1.0.1.zip` is stored outside the source repository in the sibling local folder `RoC_Workflow_GitHub_Release_Asset`. It is approximately 158 MB and exceeds GitHub's limits for ordinary web and Git repository uploads.

After the source repository is uploaded, create GitHub release tag `v1.0.1` and attach the ZIP as a release asset. Do not force the archive into normal Git history.
