# Local release assets

`RoC_Workflow_Windows_v1.0.1.zip` is stored outside the source repository. The corrected release archive is 158,010,959 bytes (approximately 151 MiB) and exceeds GitHub's limits for ordinary web and Git repository uploads.

After the source repository is uploaded, create GitHub release tag `v1.0.1` and attach the ZIP as a release asset. Do not force the archive into normal Git history.

SHA-256:

```text
6897147058A1A885FE6A85CDA08438A7BB13F89CDB79DDE69520364146DF8862
```
